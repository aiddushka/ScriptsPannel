<Window x:Class="ScriptsPannel.Views.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        SizeToContent="Height"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.Surface}">
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock x:Name="Hint" Grid.Row="0" Foreground="{DynamicResource Brush.TextMuted}" TextWrapping="Wrap" />
    <TextBox x:Name="Input" Grid.Row="1" Margin="0,10,0,14" Padding="10" FontSize="15" />

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Margin="0,0,10,0" Style="{StaticResource ChromeButton}" Content="{DynamicResource Str.Cancel}"
              IsCancel="True" Click="Cancel_Click" />
      <Button x:Name="OkBtn" Style="{StaticResource AccentButton}" Content="{DynamicResource Str.Create}"
              IsDefault="True" Click="Ok_Click" />
    </StackPanel>
  </Grid>
</Window>
